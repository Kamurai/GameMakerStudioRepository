<!--This Document is generated by GameMaker, if you edit it by hand then you do so at your own risk!-->
<object>
  <spriteName>cursor_board</spriteName>
  <solid>-1</solid>
  <visible>-1</visible>
  <depth>0</depth>
  <persistent>0</persistent>
  <parentName>&lt;undefined&gt;</parentName>
  <maskName>&lt;undefined&gt;</maskName>
  <events>
    <event eventtype="0" enumb="0">
      <action>
        <libid>1</libid>
        <id>603</id>
        <kind>7</kind>
        <userelative>0</userelative>
        <isquestion>0</isquestion>
        <useapplyto>-1</useapplyto>
        <exetype>2</exetype>
        <functionname></functionname>
        <codestring></codestring>
        <whoName>self</whoName>
        <relative>0</relative>
        <isnot>0</isnot>
        <arguments>
          <argument>
            <kind>1</kind>
            <string>//ensure "blinking" image
image_speed = .25;

//used to determine if a character has been selected
selection = 0;

//destroying variable
destroy = 0;</string>
          </argument>
        </arguments>
      </action>
    </event>
    <event eventtype="3" enumb="0">
      <action>
        <libid>1</libid>
        <id>603</id>
        <kind>7</kind>
        <userelative>0</userelative>
        <isquestion>0</isquestion>
        <useapplyto>-1</useapplyto>
        <exetype>2</exetype>
        <functionname></functionname>
        <codestring></codestring>
        <whoName>self</whoName>
        <relative>0</relative>
        <isnot>0</isnot>
        <arguments>
          <argument>
            <kind>1</kind>
            <string>//if destroy has been requested
if( destroy == 1)
{
    //destroy object
    instance_destroy();
}</string>
          </argument>
        </arguments>
      </action>
    </event>
    <event eventtype="9" enumb="70">
      <action>
        <libid>1</libid>
        <id>603</id>
        <kind>7</kind>
        <userelative>0</userelative>
        <isquestion>0</isquestion>
        <useapplyto>-1</useapplyto>
        <exetype>2</exetype>
        <functionname></functionname>
        <codestring></codestring>
        <whoName>self</whoName>
        <relative>0</relative>
        <isnot>0</isnot>
        <arguments>
          <argument>
            <kind>1</kind>
            <string>//selecting characters on the board

//if the board has focus 
if( selection_controller.boardfocus = 1 )
{

    //check position

    //player 1
        //first row
    //if this is player 1's marker 1
    if( x == 6 &amp;&amp; y == 6 )
    {
        //mark which slot the upcoming character is to be loaded into
        selection_controller.counter = 1;
    }   
    //if this is player 1's marker 2
    else if( x == 42 &amp;&amp; y == 6 )
    {
        //mark which slot the upcoming character is to be loaded into
        selection_controller.counter = 2;
    }
    //if this is player 1's marker 3
    else if( x == 78 &amp;&amp; y == 6 )
    {
        //mark which slot the upcoming character is to be loaded into
        selection_controller.counter = 3;
    }
    //if this is player 1's marker 4
    else if( x == 114 &amp;&amp; y == 6 )
    {
        //mark which slot the upcoming character is to be loaded into
        selection_controller.counter = 4;
    }
    //if this is player 1's marker 5
    else if( x == 150 &amp;&amp; y == 6 )
    {
        //mark which slot the upcoming character is to be loaded into
        selection_controller.counter = 5;
    }
    //if this is player 1's marker 6
    else if( x == 186 &amp;&amp; y == 6 )
    {
        //mark which slot the upcoming character is to be loaded into
        selection_controller.counter = 6;
    }
    //if this is player 1's marker 7
    else if( x == 222 &amp;&amp; y == 6 )
    {
        //mark which slot the upcoming character is to be loaded into
        selection_controller.counter = 7;
    }
    //if this is player 1's marker 8
    else if( x == 258 &amp;&amp; y == 6 )
    {
        //mark which slot the upcoming character is to be loaded into
        selection_controller.counter = 8;
    }
    //if this is player 1's marker 9
    else if( x == 294 &amp;&amp; y == 6 )
    {
        //mark which slot the upcoming character is to be loaded into
        selection_controller.counter = 9;
    }
    //if this is player 1's marker 10
    else if( x == 330 &amp;&amp; y == 6 )
    {
        //mark which slot the upcoming character is to be loaded into
        selection_controller.counter = 10;
    }
    //if this is player 1's marker 11
    else if( x == 366 &amp;&amp; y == 6 )
    {
        //mark which slot the upcoming character is to be loaded into
        selection_controller.counter = 11;
    }
    //if this is player 1's marker 12
    else if( x == 402 &amp;&amp; y == 6 )
    {
        //mark which slot the upcoming character is to be loaded into
        selection_controller.counter = 12;
    }
    //second row
    //if this is player 1's marker 13
    else if( x == 6 &amp;&amp; y == 42 )
    {
        //mark which slot the upcoming character is to be loaded into
        selection_controller.counter = 13;
    }   
    //if this is player 1's marker 14
    else if( x == 42 &amp;&amp; y == 42 )
    {
        //mark which slot the upcoming character is to be loaded into
        selection_controller.counter = 14;
    }
    //if this is player 1's marker 15
    else if( x == 78 &amp;&amp; y == 42 )
    {
        //mark which slot the upcoming character is to be loaded into
        selection_controller.counter = 15;
    }
    //if this is player 1's marker 16
    else if( x == 114 &amp;&amp; y == 42 )
    {
        //mark which slot the upcoming character is to be loaded into
        selection_controller.counter = 16;
    }
    //if this is player 1's marker 17
    else if( x == 150 &amp;&amp; y == 42 )
    {
        //mark which slot the upcoming character is to be loaded into
        selection_controller.counter = 17;
    }
    //if this is player 1's marker 18
    else if( x == 186 &amp;&amp; y == 42 )
    {
        //mark which slot the upcoming character is to be loaded into
        selection_controller.counter = 18;
    }
    //if this is player 1's marker 19
    else if( x == 222 &amp;&amp; y == 42 )
    {
        //mark which slot the upcoming character is to be loaded into
        selection_controller.counter = 19;
    }
    //if this is player 1's marker 20
    else if( x == 258 &amp;&amp; y == 42 )
    {
        //mark which slot the upcoming character is to be loaded into
        selection_controller.counter = 20;
    }
    //if this is player 1's marker 21
    else if( x == 294 &amp;&amp; y == 42 )
    {
        //mark which slot the upcoming character is to be loaded into
        selection_controller.counter = 21;
    }
    //if this is player 1's marker 22
    else if( x == 330 &amp;&amp; y == 42 )
    {
        //mark which slot the upcoming character is to be loaded into
        selection_controller.counter = 22;
    }
    //if this is player 1's marker 23
    else if( x == 366 &amp;&amp; y == 42 )
    {
        //mark which slot the upcoming character is to be loaded into
        selection_controller.counter = 23;
    }
    //if this is player 1's marker 24
    else if( x == 402 &amp;&amp; y == 42 )
    {
        //mark which slot the upcoming character is to be loaded into
        selection_controller.counter = 24;
    }


    //player 2
        //first row
    //if this is player 1's marker 1
    if( x == 6 &amp;&amp; y == 366 )
    {
        //mark which slot the upcoming character is to be loaded into
        selection_controller.counter = 1;
    }   
    //if this is player 1's marker 2
    else if( x == 42 &amp;&amp; y == 366 )
    {
        //mark which slot the upcoming character is to be loaded into
        selection_controller.counter = 2;
    }
    //if this is player 1's marker 3
    else if( x == 78 &amp;&amp; y == 366 )
    {
        //mark which slot the upcoming character is to be loaded into
        selection_controller.counter = 3;
    }
    //if this is player 1's marker 4
    else if( x == 114 &amp;&amp; y == 366 )
    {
        //mark which slot the upcoming character is to be loaded into
        selection_controller.counter = 4;
    }
    //if this is player 1's marker 5
    else if( x == 150 &amp;&amp; y == 366 )
    {
        //mark which slot the upcoming character is to be loaded into
        selection_controller.counter = 5;
    }
    //if this is player 1's marker 6
    else if( x == 186 &amp;&amp; y == 366 )
    {
        //mark which slot the upcoming character is to be loaded into
        selection_controller.counter = 6;
    }
    //if this is player 1's marker 7
    else if( x == 222 &amp;&amp; y == 366 )
    {
        //mark which slot the upcoming character is to be loaded into
        selection_controller.counter = 7;
    }
    //if this is player 1's marker 8
    else if( x == 258 &amp;&amp; y == 366 )
    {
        //mark which slot the upcoming character is to be loaded into
        selection_controller.counter = 8;
    }
    //if this is player 1's marker 9
    else if( x == 294 &amp;&amp; y == 366 )
    {
        //mark which slot the upcoming character is to be loaded into
        selection_controller.counter = 9;
    }
    //if this is player 1's marker 10
    else if( x == 330 &amp;&amp; y == 366 )
    {
        //mark which slot the upcoming character is to be loaded into
        selection_controller.counter = 10;
    }
    //if this is player 1's marker 11
    else if( x == 366 &amp;&amp; y == 366 )
    {
        //mark which slot the upcoming character is to be loaded into
        selection_controller.counter = 11;
    }
    //if this is player 1's marker 12
    else if( x == 402 &amp;&amp; y == 366 )
    {
        //mark which slot the upcoming character is to be loaded into
        selection_controller.counter = 12;
    }
    //second row
    //if this is player 1's marker 13
    else if( x == 6 &amp;&amp; y == 402 )
    {
        //mark which slot the upcoming character is to be loaded into
        selection_controller.counter = 13;
    }   
    //if this is player 1's marker 14
    else if( x == 42 &amp;&amp; y == 402 )
    {
        //mark which slot the upcoming character is to be loaded into
        selection_controller.counter = 14;
    }
    //if this is player 1's marker 15
    else if( x == 78 &amp;&amp; y == 402 )
    {
        //mark which slot the upcoming character is to be loaded into
        selection_controller.counter = 15;
    }
    //if this is player 1's marker 16
    else if( x == 114 &amp;&amp; y == 402 )
    {
        //mark which slot the upcoming character is to be loaded into
        selection_controller.counter = 16;
    }
    //if this is player 1's marker 17
    else if( x == 150 &amp;&amp; y == 402 )
    {
        //mark which slot the upcoming character is to be loaded into
        selection_controller.counter = 17;
    }
    //if this is player 1's marker 18
    else if( x == 186 &amp;&amp; y == 402 )
    {
        //mark which slot the upcoming character is to be loaded into
        selection_controller.counter = 18;
    }
    //if this is player 1's marker 19
    else if( x == 222 &amp;&amp; y == 402 )
    {
        //mark which slot the upcoming character is to be loaded into
        selection_controller.counter = 19;
    }
    //if this is player 1's marker 20
    else if( x == 258 &amp;&amp; y == 402 )
    {
        //mark which slot the upcoming character is to be loaded into
        selection_controller.counter = 20;
    }
    //if this is player 1's marker 21
    else if( x == 294 &amp;&amp; y == 402 )
    {
        //mark which slot the upcoming character is to be loaded into
        selection_controller.counter = 21;
    }
    //if this is player 1's marker 22
    else if( x == 330 &amp;&amp; y == 402 )
    {
        //mark which slot the upcoming character is to be loaded into
        selection_controller.counter = 22;
    }
    //if this is player 1's marker 23
    else if( x == 366 &amp;&amp; y == 402 )
    {
        //mark which slot the upcoming character is to be loaded into
        selection_controller.counter = 23;
    }
    //if this is player 1's marker 24
    else if( x == 402 &amp;&amp; y == 402 )
    {
        //mark which slot the upcoming character is to be loaded into
        selection_controller.counter = 24;
    }
    
    //selection_controller.menufocus = 1;
    instance_create(mkr_list.x, mkr_list.y, class_selector);
    
    //create arrow and shift focus to the menu
    selection_controller.menufocus = 1;
    
    
}</string>
          </argument>
        </arguments>
      </action>
    </event>
    <event eventtype="9" enumb="68">
      <action>
        <libid>1</libid>
        <id>603</id>
        <kind>7</kind>
        <userelative>0</userelative>
        <isquestion>0</isquestion>
        <useapplyto>-1</useapplyto>
        <exetype>2</exetype>
        <functionname></functionname>
        <codestring></codestring>
        <whoName>self</whoName>
        <relative>0</relative>
        <isnot>0</isnot>
        <arguments>
          <argument>
            <kind>1</kind>
            <string>//de-selecting characters on the board

//if the board has focus 
if( selection_controller.boardfocus = 1 )
{
    //if no selection has been made AND the space is not empty (filled with a character)
    if( selection == 0 &amp;&amp; place_meeting(x, y, character) == true )
    {   
        //set the selection
        //selection = 1;
        
        //check location
            //based on that location
            
            //remove loaded character information
            
            //signal to destroy character
            
            //decrement playertracker
        
    }
}</string>
          </argument>
        </arguments>
      </action>
    </event>
    <event eventtype="9" enumb="40">
      <action>
        <libid>1</libid>
        <id>603</id>
        <kind>7</kind>
        <userelative>0</userelative>
        <isquestion>0</isquestion>
        <useapplyto>-1</useapplyto>
        <exetype>2</exetype>
        <functionname></functionname>
        <codestring></codestring>
        <whoName>self</whoName>
        <relative>0</relative>
        <isnot>0</isnot>
        <arguments>
          <argument>
            <kind>1</kind>
            <string>//if the board has focus
if( selection_controller.boardfocus == 1)
{
    //if there not a border in the way (selecting characters)
    if( place_meeting(x, y + 1, border) == false )
    {
        y = y + 36;
    }
    
    
    
    
    
}</string>
          </argument>
        </arguments>
      </action>
    </event>
    <event eventtype="9" enumb="39">
      <action>
        <libid>1</libid>
        <id>603</id>
        <kind>7</kind>
        <userelative>0</userelative>
        <isquestion>0</isquestion>
        <useapplyto>-1</useapplyto>
        <exetype>2</exetype>
        <functionname></functionname>
        <codestring></codestring>
        <whoName>self</whoName>
        <relative>0</relative>
        <isnot>0</isnot>
        <arguments>
          <argument>
            <kind>1</kind>
            <string>//if the board has focus
if( selection_controller.boardfocus == 1)
{
    //if there not a border in the way (selecting characters)
    if( place_meeting(x+1, y, border) == false )
    {
        x = x + 36;
    }
    
    
    
    
    
}</string>
          </argument>
        </arguments>
      </action>
    </event>
    <event eventtype="9" enumb="38">
      <action>
        <libid>1</libid>
        <id>603</id>
        <kind>7</kind>
        <userelative>0</userelative>
        <isquestion>0</isquestion>
        <useapplyto>-1</useapplyto>
        <exetype>2</exetype>
        <functionname></functionname>
        <codestring></codestring>
        <whoName>self</whoName>
        <relative>0</relative>
        <isnot>0</isnot>
        <arguments>
          <argument>
            <kind>1</kind>
            <string>//if the board has focus
if( selection_controller.boardfocus == 1)
{
    //if there not a border in the way (selecting characters)
    if( place_meeting(x, y - 1, border) == false )
    {
        y = y - 36;
    }
    
    
    
    
    
}</string>
          </argument>
        </arguments>
      </action>
    </event>
    <event eventtype="9" enumb="37">
      <action>
        <libid>1</libid>
        <id>603</id>
        <kind>7</kind>
        <userelative>0</userelative>
        <isquestion>0</isquestion>
        <useapplyto>-1</useapplyto>
        <exetype>2</exetype>
        <functionname></functionname>
        <codestring></codestring>
        <whoName>self</whoName>
        <relative>0</relative>
        <isnot>0</isnot>
        <arguments>
          <argument>
            <kind>1</kind>
            <string>//if the board has focus
if( selection_controller.boardfocus == 1)
{
    //if there not a border in the way (selecting characters)
    if( place_meeting(x-1, y, border) == false )
    {
        x = x - 36;
    }
    
    
    
    
    
}</string>
          </argument>
        </arguments>
      </action>
    </event>
    <event eventtype="10" enumb="70">
      <action>
        <libid>1</libid>
        <id>603</id>
        <kind>7</kind>
        <userelative>0</userelative>
        <isquestion>0</isquestion>
        <useapplyto>-1</useapplyto>
        <exetype>2</exetype>
        <functionname></functionname>
        <codestring></codestring>
        <whoName>self</whoName>
        <relative>0</relative>
        <isnot>0</isnot>
        <arguments>
          <argument>
            <kind>1</kind>
            <string>//the focus is coming back from the menu
    //if the selection has been removed by the menu
if( selection == 0)
{
    //selection_controller.boardfocus = 1;
}


//the focus is going from the board to the menu
    //if the focus is not on the board
if( selection_controller.menufocus == 1)
{
    selection_controller.boardfocus = 0;
}</string>
          </argument>
        </arguments>
      </action>
    </event>
  </events>
  <PhysicsObject>0</PhysicsObject>
  <PhysicsObjectSensor>0</PhysicsObjectSensor>
  <PhysicsObjectShape>0</PhysicsObjectShape>
  <PhysicsObjectDensity>0.5</PhysicsObjectDensity>
  <PhysicsObjectRestitution>0.100000001490116</PhysicsObjectRestitution>
  <PhysicsObjectGroup>0</PhysicsObjectGroup>
  <PhysicsObjectLinearDamping>0.100000001490116</PhysicsObjectLinearDamping>
  <PhysicsObjectAngularDamping>0.100000001490116</PhysicsObjectAngularDamping>
  <PhysicsObjectFriction>0.200000002980232</PhysicsObjectFriction>
  <PhysicsObjectAwake>-1</PhysicsObjectAwake>
  <PhysicsObjectKinematic>0</PhysicsObjectKinematic>
  <PhysicsShapePoints/>
</object>
